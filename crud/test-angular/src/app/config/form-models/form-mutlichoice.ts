/**
 * Interface spécifique pour le cas où une entité a besoin peut posséder une liste d'entités associées
 * dans des quantités différentes.
 * */
export interface MultiEntity {
  nom:string,
  nb:number
}
