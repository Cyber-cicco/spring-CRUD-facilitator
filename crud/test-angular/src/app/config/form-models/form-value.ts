import {MultiEntity} from "./form-mutlichoice";

/**
 * Type correspondant aux types que peuvent prendre les champs d'un formulaire.
 * */
export type FormValue = string | string[] | Date | number | MultiEntity[]
